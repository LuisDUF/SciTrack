const PARTICIPANTE = {
    idParticipante:,
    nombre:,
    apellidoPaterno:,
    apellidoMaterno:,
    correo:,
    contraseña:,
    telefono:,
    Equipo_idEquipo:1,
    Dependencia_idDependencia:,
}

IDEA: Equipo 1 internamente se manejará como "Sin equipo"

const elementoSeleccionado = ARREGLO.find(elemento => elemento.atributo == valor);